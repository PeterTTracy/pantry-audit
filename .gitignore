node_modules/
dist/
data/
uploads/
*.log
.DS_Store

# OneDrive multi-machine sync conflict copies — never commit these
*-DESKTOP-*

# Local cloud backups from the migration script (not source)
scripts/backups/
